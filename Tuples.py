
coordinates = (4,5)
# coordinates[1] = 10 will produce an error, imutable
# Tuples are variables defined by parenthesis
# Tuples are imutable, cannot be changed or modified
# lists =  [], tuples = ()
# create a list of tuples

coordinates2 = [(4,5), (6,7), (80, 34)]
print(coordinates[0])